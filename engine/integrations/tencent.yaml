vendor_name: "tencent"
provider: "tencent"                    # Core routing discriminator token
auth_flow:
  type: "tencent_native_auth"        # Evaluates environment credential chains at runtime
  token_env: "TENCENT_ACCESS_TOKEN"  # Local fallback environment override token
endpoints: {}                      # To be populated during platform onboarding modeling
