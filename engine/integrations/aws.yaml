vendor_name: "aws"
provider: "aws"                    # Core routing discriminator token
auth_flow:
  type: "aws_native_auth"        # Evaluates environment credential chains at runtime
  token_env: "AWS_ACCESS_TOKEN"  # Local fallback environment override token
endpoints: {}                      # To be populated during platform onboarding modeling
