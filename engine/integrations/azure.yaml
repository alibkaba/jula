vendor_name: "azure"
provider: "azure"                    # Core routing discriminator token
auth_flow:
  type: "azure_native_auth"        # Evaluates environment credential chains at runtime
  token_env: "AZURE_ACCESS_TOKEN"  # Local fallback environment override token
endpoints: {}                      # To be populated during platform onboarding modeling
