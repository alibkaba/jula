# ──────────────────────────────────────────────────────────────
# Jula Evidence Collector – User Configuration
# ──────────────────────────────────────────────────────────────
# Copy this file to terraform.tfvars and fill in your values.
#
#   cp terraform.tfvars.example terraform.tfvars
#
# Do NOT commit terraform.tfvars to version control because it
# may contain sensitive or environment-specific values.
# ──────────────────────────────────────────────────────────────

project_id           = "your-gcp-project-id"
region               = "us-central1"
docker_image         = "us-central1-docker.pkg.dev/your-gcp-project-id/jula-evidence-collector/jula:latest"
evidence_bucket_name = "jula-evidence-output-your-gcp-project-id"

# Optional overrides (defaults shown):
# service_account_id    = "jula-runner"
# signing_key_secret_id = "jula-signing-key"
# scheduler_cron        = "0 2 * * 1"
# scheduler_timezone    = "Etc/UTC"
